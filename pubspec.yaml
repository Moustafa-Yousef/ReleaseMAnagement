name: release_manager  
description: A CLI tool for analyzing code changes
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  http: ^1.3.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
